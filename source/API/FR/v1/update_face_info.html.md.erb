> To update face details use this code :

```python
import base64
import requests

url = 'https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_info'
header = {"Content-Type":"application/json"}

myobj = {
            "key": "Your Project Key",
            "id" : "Face Image ID",
            "first_name" : "John",
            "last_name" : "Doe"
        }
x = requests.post(url, json = myobj, headers=header)
```

```javascript
var url = "https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_info";

var data = JSON.stringify(
    {
        "key": "Your Project Key",
        "id" : "Face Image ID",
        "first_name" : "John",
        "last_name" : "Doe"
    }
);

var xhr = new XMLHttpRequest();
xhr.withCredentials = true;

xhr.addEventListener("readystatechange", function() {
  if(this.readyState === 4) {
    console.log(this.responseText);
  }
});

xhr.open("POST", url);
xhr.setRequestHeader("Content-Type", "application/json");

xhr.send(data);
```

> Response Example

```
{
    "code": 200,
    "message": "Success",
    "status": "OK"
}
```

This endpoint updates face details using face-id.

### Request Information
`POST - /api/fr/v1/update_face_info`

### Headers
|Key|Value|
|-------|--------|
|Content-type|application/json|

### Requests parameters
|Field|Type|Description|
|-------|-------|--------|
|key|string|Customer project authentication|
|id|string|Face-id returned with add_face api|
|first_name|string|Firstname of the user|
|last_name|string|Lasttname of the user|