> To search a face use this code :

```python
import base64
import requests

url = 'https://facial-api.globalvoxinc.com:8443/api/fr/v1/search_face'
header = {"Content-Type":"application/json"}

with open("Your Image Path", "rb") as f:
    img_str = base64.b64encode(f.read()).decode("utf-8")

myobj = {
            "key": "Your Project Key",
            "image_string" : img_str
        }
x = requests.post(url, json = myobj, headers=header)
```

```javascript
var url = "https://facial-api.globalvoxinc.com:8443/api/fr/v1/search_face";

var data = JSON.stringify(
    {
        "key":"Your Project Key",
        "image_string":"data:image/jpeg;base64,/9j/4AA..."
    }
);

var xhr = new XMLHttpRequest();
xhr.withCredentials = true;

xhr.addEventListener("readystatechange", function() {
  if(this.readyState === 4) {
    console.log(this.responseText);
  }
});

xhr.open("POST", url);
xhr.setRequestHeader("Content-Type", "application/json");

xhr.send(data);
```

> Response Example

```
{
    "code": 200,
    "data": [
        {
            "_id": "84ecbe39-a146-46b9-ab1d-d9082639b9db",
            "first_name": "John",
            "img_url": null,
            "last_name": "Doe",
            "mapping_id": "1",
            "similarity": 0.884518027305603,
            "status": 1
        },
        {
            "_id": "301f4318-59ce-47f8-8a11-b4548165c1ec",
            "first_name": "John",
            "img_url": null,
            "last_name": "Doe",
            "mapping_id": "1",
            "similarity": 0.6752777099609375,
            "status": 1
        },
        {
            "_id": "bedb4a3c-dc18-438e-933a-5cfebcf34ea6",
            "first_name": "John",
            "img_url": null,
            "last_name": "Doe",
            "mapping_id": "1",
            "similarity": 0.6315699815750122,
            "status": 1
        }
    ],
    "message": "Success",
    "status": "OK"
}
```
This endpoint is used to search face from registered images. It detects and searches the biggest face from the image. In return it gives id, similarity, first_name, last_name, status of 3 best similar faces.
If more faces of a user is registered then this can be used to show the confidence of the input image.

Similarity – It specifies how similar the given image is from registered images. Recommended similarity is 0.6, any similarity greater than or equal to 0.5 is considered recognized face otherwise unknown face.

<aside class="notice">
The minimum height and width of the face should be 180px.
</aside>

### Request Information
`POST - /api/fr/v1/search_face`

### Headers
|Key|Value|
|-------|--------|
|Content-type|application/json|

### Requests parameters
|Field|Type|Description|
|-------|-------|--------|
|key|string|Customer project authentication|
|image_string| string (base64/url)| Base64/Url string of image containing face you want to search |