> To update face details using mapping_id use this code :

```python
import base64
import requests

url = 'https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_info_mid'
header = {"Content-Type":"application/json"}

myobj = {
            'key': "Your Project Key",
            'mapping_id' : "Face Grouping ID",
            'first_name' : "John",
            'last_name' : "Doe"
        }
x = requests.post(url, json = myobj, headers=header)
```

```javascript
var url = "https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_info_mid";

var data = JSON.stringify(
    {
        'key': "Your Project Key",
        'mapping_id' : "Face Grouping ID",
        'first_name' : "John",
        'last_name' : "Doe"
    }
);

var xhr = new XMLHttpRequest();
xhr.withCredentials = true;

xhr.addEventListener("readystatechange", function() {
  if(this.readyState === 4) {
    console.log(this.responseText);
  }
});

xhr.open("POST", url);
xhr.setRequestHeader("Content-Type", "application/json");

xhr.send(data);
```

> Response Example

```
{
    "code": 200,
    "message": "Success",
    "status": "OK"
}
```

This endpoint updates face details using mapping_id.

### Request Information
`POST - /api/fr/v1/update_face_info_mid`

### Headers
|Key|Value|
|-------|--------|
|Content-type|application/json|

### Requests parameters
|Field|Type|Description|
|-------|-------|--------|
|key|string|Customer project authentication|
|mapping_id|string|Mapping-id that groups faces provided at the time of face registration|
|first_name|string|Firstname of the user|
|last_name|string|Lastname of the user|