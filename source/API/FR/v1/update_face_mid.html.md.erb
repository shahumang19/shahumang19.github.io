> To update face details use this code :

```python
import base64
import requests

url = 'https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_mid'
header = {"Content-Type":"application/json"}

myobj = {
          "key": "Your Project Key",
          "id_list" : "List Of Face IDs",
          "mapping_id" : "Face Group ID"
        }
x = requests.post(url, json = myobj, headers=header)
```

```javascript
var url = "https://facial-api.globalvoxinc.com:8443/api/fr/v1/update_face_mid";

var data = JSON.stringify(
    {
      "key": "Your Project Key",
      "id_list" : "[List Of Face IDs]",
      "mapping_id" : "Face Group ID"
    }
);

var xhr = new XMLHttpRequest();
xhr.withCredentials = true;

xhr.addEventListener("readystatechange", function() {
  if(this.readyState === 4) {
    console.log(this.responseText);
  }
});

xhr.open("POST", url);
xhr.setRequestHeader("Content-Type", "application/json");

xhr.send(data);
```

> Response Example

```
{
    "code": 200,
    "message": "Success",
    "status": "OK"
}
```

This endpoint updates mapping_id based on specified face ids.

### Request Information
`POST - /api/fr/v1/update_face_mid`

### Headers
|Key|Value|
|-------|--------|
|Content-type|application/json|

### Requests parameters
|Field|Type|Description|
|-------|-------|--------|
|key|string|Customer project authentication|
|id_list|array|List of face ids|
|mapping_id|string|Mapping-id that groups faces provided at the time of face registration|