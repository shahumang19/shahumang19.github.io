> To delete a face use this code :

```python
import requests
url = 'https://facial-api.globalvoxinc.com:8443/api/fr/v1/delete_face'
header = {"Content-Type":"application/json"}
myobj = {
            "key": "Your Project Key",
            "id_list" : ["List of your Face Ids"]
        }
x = requests.post(url, json = myobj, headers= header)
```

```javascript
var url = "https://facial-api.globalvoxinc.com:8443/api/fr/v1/delete_face";

var data = JSON.stringify(
    {
        "key":"Your Project Key",
        "id_list":["List of your Face Ids"]
    }
);

var xhr = new XMLHttpRequest();
xhr.withCredentials = true;

xhr.addEventListener("readystatechange", function() {
  if(this.readyState === 4) {
    console.log(this.responseText);
  }
});

xhr.open("POST", url);
xhr.setRequestHeader("Content-Type", "application/json");

xhr.send(data);
```

> Response Example

```
{
    "code": 200,
    "message": "Success",
    "status": "OK"
}
```

This endpoint deletes a face based on the Face-id provided by add_face api. It will delete multiple faces based on array of multiple face-ids provided. 

### Request Information
`POST - /api/fr/v1/delete_face`

### Headers
|Key|Value|
|-------|--------|
|Content-type|application/json|

### Requests parameters
|Field|Type|Description|
|-------|-------|--------|
|key|string|Customer project authentication|
|id_list|array|Array of Face-ids(Face-id returned with add_face api) to delete 1 or multiple faces| 